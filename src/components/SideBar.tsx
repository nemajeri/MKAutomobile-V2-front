import Select from "react-select";

export const SideBar = () => {
  return (
    <div>
      <Select options={[]} />
      <Select options={[]} />
      <Select options={[]} />
      <Select options={[]} />
      <Select options={[]} />
      <Select options={[]} />
    </div>
  );
};
